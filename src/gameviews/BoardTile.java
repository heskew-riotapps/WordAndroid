package gameviews;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

public class BoardTile extends View {

	public BoardTile(Context context, AttributeSet attrs) {
		super(context, attrs);
		// TODO Auto-generated constructor stub
		
		//determine if onDrop should be exposed
		//determine if onDrag should be exposed
		//double tap should zoom with this view in focus, if in full board view
		//double tap should shrink, if in zoom view
		
	}

}
